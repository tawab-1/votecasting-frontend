import {Content} from 'antd/es/layout/layout';
import styled from 'styled-components';

export const CommonContentWrapper = styled(Content)`
  padding: 0px 25px;
  overflow-y: auto;
  overflow-x: hidden;
`;
